var MIN_AMOUNT = 1000;

class SavingsAccount {

    withdrawMoney(amount) {
        if(amount < MIN_AMOUNT) {
            // do something
        }
    }

    transferMoney(amount) {
        if(amount < MIN_AMOUNT) {
            // do something
        }
    }
}